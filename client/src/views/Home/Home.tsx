import { Center } from "@chakra-ui/layout";
import { Button, Input } from "@chakra-ui/react";

type Props = {};

const HomeView = (props: Props) => {
	return <Center>Home View</Center>;
};

export default HomeView;
